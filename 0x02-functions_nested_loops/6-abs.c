
#include "main.h"
/**
 * _abs - omputer the absolute value of an inteher
 *
 *@n: numbert is being tested
 *
 * Return: Always 0
 */
int _abs(int n)
{

	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}

	return (0);
}
